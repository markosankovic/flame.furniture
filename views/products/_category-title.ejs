<div class="category-title"><% if (category.parent) { %><%- link_to(__(`category.${category.parent.name}`), `/products/${category.parent.slug}`) %> / <% } %><%= __(`category.${category.name}`) %></div>